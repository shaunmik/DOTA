using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Spell : MonoBehaviour {

/*	public GameObject[] Prefabs;
	public ArrayList scriptList;

	// TODO: find a way to unify all spell's classes 
	// (when we work on spells, inherit from a same class)

	//private FireBaseScript firePrefabScript;
	// private WaterBaseScript waterPrefabScript;
	// private EarthBaseScript earthPrefabScript;
	// private WindBaseScript windPrefabScript;

	void Start () {
		firePrefabScript = spellPrefab.GetComponent<FireConstantBaseScript>();
	}
	
	// TODO: return corresponding spell script...
	// need to unify the type of each prefab script
	public GameObject instantiateSpell(int spellNum) {
		if (spellNum == (int) SpellController.Spells.fire) {
			return GameObject.Instantiate(Prefabs[0]);
		}
	}*/


}
